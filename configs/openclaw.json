{
  "agents": {
    "defaults": {
      "model": {
        "primary": "{{PROVIDER}}/{{MODEL}}",
        "fallbacks": []
      }
    }
  },
  "models": {
    "mode": "merge",
    "providers": {}
  }
}
