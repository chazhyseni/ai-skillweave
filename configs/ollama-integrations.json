{
  "_comment": "Opt-in Ollama launch selections only; no models are downloaded. setup-ollama-config.sh merges these selections without replacing other integrations.",
  "_verified_on": "2026-09-25",
  "_sources": ["https://ollama.com/library/qwen3.5", "https://ollama.com/library/qwen3.8", "https://ollama.com/library/ornith"],
  "_note": "qwen3.5:4b is a modest extraction starting point, not a universal agent recommendation. Choose a tool-capable model that fits RAM plus context/KV cache. Context claims are not runtime allocations.",
  "integrations": {
    "claude": {"models": ["{{MODEL}}"]},
    "cline": {"models": ["{{MODEL}}"]},
    "codex": {"models": ["{{MODEL}}"]},
    "openclaw": {"models": ["{{MODEL}}"]},
    "pi": {"models": ["{{MODEL}}"]}
  }
}
